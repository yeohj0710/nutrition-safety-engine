import{describe,expect,it}from"vitest";import{registryLinkBundles,registryLinkTotals}from"@/src/lib/registry-link-review";describe("registry link prereview",()=>{it("covers all candidates",()=>{expect(registryLinkBundles.reduce((n,b)=>n+b.count,0)).toBe(500)});it("keeps human boundary",()=>{expect(registryLinkTotals.human_link_decisions).toBe(0);expect(registryLinkTotals.registry_study_links_claim_allowed).toBe(false)})});
