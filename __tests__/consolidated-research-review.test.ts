import{describe,expect,it}from"vitest";import{consolidatedBundles,consolidatedTotals}from"@/src/lib/consolidated-research-review";describe("consolidated review",()=>{it("bundles remaining work once",()=>expect(consolidatedBundles).toHaveLength(3));it("preserves boundaries",()=>{expect(consolidatedTotals.koreamed_records).toBe(62);expect(consolidatedTotals.koreamed_pubmed_link_candidates).toBe(35);expect(consolidatedTotals.final_search_claim_allowed).toBe(false)})});
