import{describe,expect,it}from"vitest";import{registryBundles,registryTotals}from"@/src/lib/registry-screening-review";describe("registry prereview",()=>{it("covers 207 records across five questions",()=>{expect(registryBundles).toHaveLength(5);expect(registryBundles.reduce((n,b)=>n+b.records,0)).toBe(207)});it("keeps human boundary open",()=>{expect(registryTotals.human_registry_decisions).toBe(0);expect(registryTotals.independent_reviewers_completed).toBe(0);expect(registryTotals.final_registry_screening_claim_allowed).toBe(false)})});
