# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.venv/
/.npm-cache/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
__pycache__/
.pytest_cache/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.next-*.log
tmp-next-dev.*.log
codex-start-*.log
.playwright-cli/
output/

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# search pipeline local browser session
data/systematic_search/.browser/


# large/copyright-sensitive public search payloads stay local; hashes and metadata are tracked

# Claude Code local tooling config
/.claude/

# v4.0 large payloads stay local; hashes and metadata are tracked.
# Inventory with sizes and SHA-256: research/logs/v40_local_only_manifest.json
#   - efetch XML: same volume/copyright reason as research/searches/ above (1.1 GB)
#   - evidence_map.csv: 114.5 MiB, over GitHub's 100 MiB per-file hard limit.
#     Rebuildable from the efetch XML; hash in data/curated_v4/corpus_manifest.json.
#   - v40_agent/batches/: screening inputs, reproducible from evidence_map.csv +
#     batch_index.json, which carries input_sha256 for all 196 batches.
#   - etc/failed_classifier_*/: discarded classifier generations kept as local
#     evidence of the rejection process; hashes tracked in the manifest.
_보관/research/searches_v4/*/pubmed/*/efetch_*.xml
_보관/data/curated_v4/evidence_map.csv
_보관/research/screening/v40_agent/batches/
_보관/research/screening/v40_agent/etc/failed_classifier_*/

# 재수집 트랙(v41) 대용량 산출물. 아래 두 CSV 는 82MiB 와 18MiB 라 저장소를
# 부풀리고, 앱이 읽지 않는 중간 파일이다. tools/build/build_site.py 로 다시
# 만든다. 코퍼스 입력도 같은 이유로 뺀다(472MiB).
data/corpus/
research/systematic_review/picos_extraction.csv
research/systematic_review/regex_gate.csv
# 질문별로 쪼갠 것이 배포본이다(extended_evidence/). 합본 40MB 는 쪼개기
# 입력으로만 남긴다. 콜드 요청이 6.3초였고 방문자는 한 번에 상황 하나만 본다.
research/systematic_review/extended_evidence.json
