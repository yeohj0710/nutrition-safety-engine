"use client";import{useEffect,useMemo,useState}from"react";type B={id:string;title:string;finding:string;recommendation:string};type E={bundleId:string;decision:"approve_consolidated_agent_prereview";validatedAt:string};const KEY="nutrition-safety-consolidated-remaining-review-v1";
export function ConsolidatedResearchReviewClient({bundles}:{bundles:B[]}){const[e,setE]=useState<E[]>([]),[i,setI]=useState(0);useEffect(()=>{const t=setTimeout(()=>{const s=JSON.parse(localStorage.getItem(KEY)??"[]")as E[];setE(s);const n=bundles.findIndex(b=>!s.some(x=>x.bundleId===b.id));if(n>=0)setI(n)},0);return()=>clearTimeout(t)},[bundles]);const done=useMemo(()=>new Set(e.map(x=>x.bundleId)).size===3,[e]);const a=bundles[i];function approve(){const n=[...e.filter(x=>x.bundleId!==a.id),{bundleId:a.id,decision:"approve_consolidated_agent_prereview" as const,validatedAt:new Date().toISOString()}];setE(n);localStorage.setItem(KEY,JSON.stringify(n));const j=bundles.findIndex(b=>!n.some(x=>x.bundleId===b.id));if(j>=0)setI(j)}function download(){const p={schema_version:"1.0.0",approval_type:"consolidated_remaining_agent_prereview_validation",reviewer_identity:"portal_reviewer_identity_not_captured",validation_events:e,bundles_validated:e.length,human_individual_decisions_recorded:0,independent_reviewers_completed:0,final_search_claim_allowed:false,limitation:"Bundle validation is not individual screening, linkage, independent dual review, or final search completion.",exported_at:new Date().toISOString()};const b=new Blob([JSON.stringify(p,null,2)],{type:"application/json"}),l=document.createElement("a");l.href=URL.createObjectURL(b);l.download="consolidated-remaining-research-approval.json";l.click();URL.revokeObjectURL(l.href)}return <main className="min-h-[78vh] bg-[#f9fafb] px-6 py-20 text-[#191f28] sm:px-8 sm:py-28"><div className="mx-auto max-w-[860px]"><p className="text-lg font-bold text-[#3182f6] sm:text-[22px]">남은 사전검토 통합 승인</p><h1 className="mt-3 text-[28px] font-bold leading-[1.4] sm:text-[46px]">남은 검토 항목을 한곳에 모았습니다.<br/>이 화면에서 세 번만 확인해 주세요.</h1><p className="mt-6 font-semibold leading-7 text-[#6b7684]">이후 같은 방식의 작은 승인 페이지를 반복하지 않습니다.</p>{done?<section className="mt-12 bg-white p-14 text-center"><p className="text-lg font-bold text-[#008c63]">3 / 3 완료</p><h2 className="mt-3 text-3xl font-bold">통합 사전검토가 끝났습니다.</h2><p className="mt-4 font-semibold text-[#6b7684]">실제 독립 선별·링크 확정·최종 검색 완료로 표시하지 않습니다.</p><button onClick={download} className="mt-8 rounded-2xl bg-[#191f28] px-7 py-4 font-bold text-white">승인 기록 내려받기</button></section>:<><div className="mt-12 flex items-center gap-4"><div className="h-2 flex-1 rounded-full bg-[#e5e8eb]"><div className="h-full rounded-full bg-[#3182f6]" style={{width:`${((i+1)/3)*100}%`}}/></div><strong>{i+1} / 3</strong></div><section className="mt-5 bg-white p-7 sm:p-10"><h2 className="text-3xl font-bold">{a.title}</h2><p className="mt-5 font-semibold leading-7 text-[#6b7684]">{a.finding}</p><div className="mt-8 border-y border-[#e5e8eb] py-8"><p className="text-sm font-bold text-[#3182f6]">권고안</p><p className="mt-2 text-lg font-bold leading-8">{a.recommendation}</p></div><button onClick={approve} className="mt-8 w-full rounded-2xl bg-[#3182f6] px-7 py-4 font-bold text-white">이 권고안으로 승인하고 다음</button></section></>}</div></main>}
